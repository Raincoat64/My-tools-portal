# My-tools-portal

iOS/Windows のブラウザから使う個人用ツール集。GitHub Pages 相当の静的サイト＋PWA。

## リポジトリの性質

**ビルド工程・パッケージマネージャ・依存パッケージは一切ない。** `package.json` も無い。この「依存ゼロで単体配布できる」性質は意図的なもので、壊さないこと。

- 各ツールは `tools/*.html` の **1ファイル完結**。インライン `<style>` と単一のインライン `<script>` で構成する
- JS は `/* === xxx.js === */` 形式のバナーコメントで擬似的にモジュール分割する(既存ファイルの流儀に合わせる)
- UI文言・コメントはすべて日本語
- 配色は全ツール共通の CSS カスタムプロパティ(`--bg` / `--panel-bg` / `--accent` など)。`@media (prefers-color-scheme: dark)` でダークモードに対応する。新規ツールは既存ファイルから `:root` ブロックをそのまま流用する

### ツールを追加するとき

1. `tools/` に自己完結HTMLを置く
2. `index.html` の `.card-grid` に `<a class="pick-card" href="./tools/xxx.html">` を追加する
3. `sw.js` の `APP_SHELL` にパスを追記し、**`CACHE_NAME` をバンプする**(忘れると更新が反映されない)

## tools/gaiji-maker.html — 異体字(外字)作成ツール

画像から Windows の外字(EUDC)を作り、IMEの変換候補として登録するためのツール。通信・APIは一切使わない。

**このファイルは単体で iCloud Drive に保存してローカルで使う運用もある。** そのため外部ファイルへの分割は行わないこと。`file://` で開かれる前提の配慮(クリップボードAPIのフォールバック等)も入っている。

### 現在の方式

**モノクロBMPを保存 → ペイントで開く → コピー → 外字エディター(eudcedit.exe)に貼り付け** の一本。

画面構成は Step 0(既存文字の検索)→ 1 画像選択 → 2 調整 → 3 コードポイント選択 → 4 BMP保存と貼り付け → 5 IME辞書。

### 過去に試して**却下した方式**(再提案しないこと)

ここが最も重要。同じ道を繰り返さないための記録。

1. **クリップボードへ画像を直接コピーして外字エディターに貼る** — 原理的に不可能。ブラウザは `image/png` しかクリップボードに書けず、Chromeがそれを Windows 側で 32bit の CF_DIBV5 に変換する。外字エディターのような旧来のアプリがこれを解釈できず**黒潰れする**。実機で確認済みで、ツール側からビット深度を制御する手段は存在しない。透過にしても白背景にしても解消しない。**ペイントを挟むとビットマップ形式が正規化されるため成立する**、というのが現在の方式の根拠。

2. **TrueType外字フォント(EUDC.TTE)をブラウザ内で直接生成する** — 実装・検証まで到達し、`opentype.js` と `fontkit` の双方でパースでき、字形も100%一致することを確認していた。既存 EUDC.TTE への無損失マージ(glyfバイト列をそのまま引き継ぐ方式)も動作していた。しかし**実機でのフォント登録がうまくいかず、手順も利用者に分かりにくい**ため撤去した。復活させる場合は「生成」ではなく**登録手順の課題**から再検討すること。実装自体は git 履歴に残っている(コミット `fb67f5c` で削除)。

3. **レジストリ登録用 .reg ファイルの生成** — 上記2に付随して実装したが、EUDC登録済みの環境ではファイル差し替えだけで足りるため削除した(コミット `e3913ed`)。

### 設計上の意図

- **Step 0 を先頭に置いているのは意図的**。外字はそのPCでしか表示されず、他PC・PDF・Office・Web では別の字や豆腐になる。既存のUnicode文字・互換漢字・IVSで足りるならそちらを使うべきなので、外字作成は最終手段として後段に置いている
- 出力サイズの既定 64×64 は、外字エディターの編集グリッドに合わせたもの
- 2値化は「線画=不透明な黒 / 背景=アルファ0」。`buildMonochromeBmp()` はこのアルファを見て 1bpp に変換する

### 埋め込みデータ

- **旧字体773組** — kyujitai.js (MIT, hakatashi) の `data/kyujitai.json` から抽出。**ライセンス全文を `<head>` のコメントに掲載済み。削除しないこと**
- **互換漢字** — データを持たず、起動時に `normalize("NFD")` で U+F900–FAD9 / U+2F800–2FA1D を走査して導出(1,002件)。正規分解を持たない14字(﨑 U+FA11 等)のみ手動テーブルで補完
- **人名異体字グループ** — 崎/﨑/嵜、高/髙 など、旧字体関係ではないため kyujitai に無いものを手動で作成

### 既知の制約

- **IVS検出は網羅的でない** — Canvasに描画したピクセルのハッシュ比較で判定しているため、32bitハッシュの衝突・アンチエイリアスの環境差・フォントフォールバックによる誤検出の余地がある。「このPCのフォントで表示できるもの」であって、IVDに登録された異体字の一覧ではない
- **コードポイントの使用中判定は推定** — 未登録時の豆腐字形と比較しているだけなので断定できない。UI文言も「〜の可能性」に留めてある
- **IVS走査は約3秒メインスレッドを占有する**(11書体 × 241回の描画)。進捗表示・中止操作は未実装
- CSP未設定。単一HTML・インラインスクリプト構成のため `unsafe-inline` が必要になり、有効な防御線にはならない

### 検証のしかた

ビルドが無いので、ローカル配信して headless Chromium で操作確認するのが基本。**`package.json` は追加しないこと**(依存ゼロを保つため、テスト用パッケージはリポジトリ外に入れる)。

```bash
# 配信(クリップボードAPIはセキュアコンテキストが必要なため file:// 直開きは避ける)
python3 -m http.server 8931
# → http://localhost:8931/tools/gaiji-maker.html
```

JS構文だけ確認したいとき:

```bash
node -e 'const fs=require("fs");const h=fs.readFileSync("tools/gaiji-maker.html","utf8");
fs.writeFileSync("/tmp/x.js",h.match(/<script>([\s\S]*)<\/script>/)[1]);'
node --check /tmp/x.js
```

ブラウザ操作の確認には Playwright を使う(リポジトリ外にインストールする)。**`pageerror` の監視が実質的な回帰検出になる** — 削除漏れの参照があれば即座に例外になるため。

確認すべき観点:

- **BMP出力** — バイナリを直接読み、`biBitCount=1` / `BI_RGB` / パレット(0=黒 1=白) / 下から上の行順 / 4バイト境界パディング / **デコードした画素配置が元画像と一致すること**
- **辞書ファイル** — 先頭が `FF FE`(UTF-16LE BOM)で、`読み\t語句\t品詞\r\n` の並びになっていること。IVS付き語句はサロゲートペアが正しく出ること
- **異体字検索** — 「崎」で 﨑(U+FA11)/嵜/㟢、「辺」で邊/邉 が出ること
- 回転・反転・しきい値・出力サイズの切替、コードポイントグリッドと入力欄の同期、Ctrl+V での画像取り込み

## 未確認・未解決の事項

- **ペイント経由での貼り付けが実機で成立するか未確認**。BMP一本化(`fb67f5c`)の後、まだ Windows 実機で試していない。黒潰れが続く場合は、ペイントの「ファイル」→「画像のプロパティ」で白黒に変換してからコピーする手順を追加する余地がある
- IVS走査の Web Worker 化と進捗表示・中止操作
- コードポイントグリッド(256セル)のスクリーンリーダー読み上げ負荷。仮想化が望ましい
- 組織で使う場合の、PC間でのコードポイント統一と外字台帳の仕組み

## 参考

- 技術監査レポート(セキュリティ・データ保全・Unicode/IVS・UI/UX): https://claude.ai/code/artifact/c415a604-cf18-46c6-a4ad-aa31f6b629d5
  - フォント生成を撤去したため、TrueType生成に関する指摘は現在の実装には該当しない
